# Supabase Configuration
# Get these from your Supabase project: Settings > API

VITE_SUPABASE_URL=your-project-url
VITE_SUPABASE_ANON_KEY=your-anon-key

# Plans persistence provider:
# - auto: use Supabase when configured (recommended)
# - supabase: always write directly to Supabase (A2)
# - api: always use the Express API (A1)
VITE_PLANS_PROVIDER=auto

# Express API (A1) - optional in production if using Supabase directly
# In dev, Vite proxies /plans and /health to http://localhost:3001 so VITE_API_URL can be empty.
VITE_USE_API=false
VITE_API_URL=

# Server-only (Express) configuration
# Comma-separated list of allowed origins for CORS.
ALLOWED_ORIGINS=http://localhost:8080,http://localhost:5173,http://localhost:3000
